/*#include <gtest/gtest.h>

TEST(BST_tests, create_BST_without_records)
{
	FAIL();
}

TEST(BST_tests, create_BST_with_records)
{
	FAIL();
}

TEST(BST_tests, add) // one and some elements
{
	FAIL();
}

TEST(BST_tests, delete_TODO) // one and some elements
{
	FAIL();
}

TEST(BST_tests, delete_last_element_TODO) // one and some elements
{
	FAIL();
}

TEST(BST_tests, delete_in_void_BST_TODO) // one and some elements
{
	FAIL();
}

TEST(BST_tests, search_TODO) // zero, one and some elements
{
	FAIL();
}

TEST(BST_tests, print_TODO) // zero, one and some elements
{
	FAIL();
}

TEST(BST_tests, read_data_from_file_TODO) // zero, one and some elements
{
	FAIL();
}

TEST(BST_tests, generate_random_data_TODO) // zero, one and some elements
{
	FAIL();
}

TEST(BST_tests, bad_data_range_TODO) // all previous functions
{
	FAIL();
}*/