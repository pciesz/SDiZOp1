#include <gtest/gtest.h>

TEST(heap_tests, create_heap_without_records)
{
	FAIL();
}

TEST(heap_tests, create_heap_with_records)
{
	FAIL();
}

TEST(heap_tests, add_element_TODO) // one and some elements
{
	FAIL();
}

TEST(heap_tests, delete_from_top_TODO) // one and some elements
{
	FAIL();
}

TEST(heap_tests, delete_last_element_TODO) // one and some elements
{
	FAIL();
}

TEST(heap_tests, delete_in_void_heap_TODO) // one and some elements
{
	FAIL();
}

TEST(heap_tests, search_TODO) // zero, one and some elements
{
	FAIL();
}

TEST(heap_tests, print_TODO) // zero, one and some elements
{
	FAIL();
}

TEST(heap_tests, read_data_from_file_TODO) // zero, one and some elements
{
	FAIL();
}

TEST(heap_tests, generate_random_data_TODO) // zero, one and some elements
{
	FAIL();
}

TEST(heap_tests, bad_data_range_TODO) // all previous functions
{
	FAIL();
}