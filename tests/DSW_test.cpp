// TODO
/*#include <gtest/gtest.h>
#include "../h/BST.h"

TEST(DSW_tests, rotate_left) {
  vector<key_type> temp{555, 444, 333, 2, 1, 0, -1, -111, -222, -333, -444, -555};
  BST x(temp);

  x.print();

  cout << "Rotacja w lewo, -555" << endl;
  x.rotate_left(-555);
  x.print();

  cout << "Rotacja w lewo, -555" << endl;
  x.rotate_left(-555);
  x.print();

  cout << "Rotacja w lewo, -222" << endl;
  x.rotate_left(-222);
  x.print();

  cout << "Rotacja w lewo, 1" << endl;
  x.rotate_left(1);
  x.print();

  cout << "Rotacja w lewo, 1" << endl;
  x.rotate_left(1);
  x.print();

  cout << "Rotacja w lewo, 333" << endl;
  x.rotate_left(333);
  x.print();

  cout << "Rotacja w lewo, 555" << endl;
  x.rotate_left(555);
  x.print();

  cout << "Rotacja w lewo, 555" << endl;
  x.rotate_left(555);
  x.print();
}

TEST(DSW_tests, rotate_right) {
  vector<key_type> temp{-555, -444, -333, -222, -111, -1, 0, 1, 2, 333, 444, 555};

  BST x(temp);

  x.print();

  cout << "Rotacja w prawo, -555" << endl;
  x.rotate_right(-555);
  x.print();

  cout << "Rotacja w prawo, -555" << endl;
  x.rotate_right(-555);
  x.print();

  cout << "Rotacja w prawo, -222" << endl;
  x.rotate_right(-222);
  x.print();

  cout << "Rotacja w prawo, 1" << endl;
  x.rotate_right(1);
  x.print();

  cout << "Rotacja w prawo, 1" << endl;
  x.rotate_right(1);
  x.print();

  cout << "Rotacja w prawo, 333" << endl;
  x.rotate_right(333);
  x.print();

  cout << "Rotacja w prawo, 555" << endl;
  x.rotate_right(555);
  x.print();

  cout << "Rotacja w prawo, 555" << endl;
  x.rotate_right(555);
  x.print();

}

TEST(DSW_tests, some_rotations) {
  FAIL();
}

TEST(DSW_tests, balancing_tree) {
  FAIL();
}

TEST(DSW_tests, balancing_big_tree) {
  FAIL();
}

TEST(DSW_tests, balancing_void_tree) {
  FAIL();
}*/